/* Portable product capability: the public Docker image has no repository,
   deployment toolchain or authorized Hermes customization runtime. */
export const AI_CUSTOMIZING_ENABLED = false;

/* Room-image generation is portable and uses explicit ChatGPT or API-key
   authorization inside the product. */
export const ROOM_IMAGE_WIZARD_ENABLED = true;
